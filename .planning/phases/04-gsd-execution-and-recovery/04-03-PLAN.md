---
phase: 04-gsd-execution-and-recovery
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: ["test/integration/bootstrap-managed-repo.test.ts", "test/integration/bootstrap-planning.test.ts", "scripts/smoke/bootstrap-managed-repo.sh", "scripts/smoke/bootstrap-planning.sh", "scripts/smoke/bootstrap-execution.sh", "scripts/smoke/resume-execution.sh"]
autonomous: true
requirements: ["EXEC-02", "EXEC-03", "EXEC-04", "EXEC-05"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260412T012001Z
generated_at: 2026-04-12T01:20:01.688Z
must_haves:
  truths:
    - "Live yolo bootstrap covers planning plus managed execution with a deterministic runner stub."
    - "Resume and recovery paths are covered by both integration tests and smoke scripts."
    - "Existing managed-bootstrap coverage stays green after the new execution behavior lands."
  artifacts:
    - path: "scripts/smoke/bootstrap-execution.sh"
      provides: "smoke coverage for yolo auto-start execution"
      contains: "execution-state.json"
    - path: "scripts/smoke/resume-execution.sh"
      provides: "smoke coverage for failed-run recovery"
      contains: "resume"
    - path: "test/integration/bootstrap-managed-repo.test.ts"
      provides: "managed bootstrap coverage updated for phase-4 execution behavior"
      contains: "execution-summary"
  key_links:
    - from: "scripts/smoke/bootstrap-execution.sh"
      to: "src/cli/commands/bootstrap.ts"
      via: "yolo bootstrap auto-start path"
      pattern: "bootstrap"
    - from: "scripts/smoke/resume-execution.sh"
      to: "src/cli/commands/resume.ts"
      via: "explicit recovery path"
      pattern: "resume"
---

<objective>
Verify the managed execution and recovery loops end to end, including the existing bootstrap flows that now auto-start yolo execution.

Purpose: Phase 4 must prove that the new runner boundary, checkpoint persistence, and resume flow work together in realistic CLI scenarios.
Output: Updated integration coverage, new smoke scripts for execution and recovery, and passing verification across the changed CLI flows.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gsd-execution-and-recovery/04-CONTEXT.md
@.planning/phases/04-gsd-execution-and-recovery/04-RESEARCH.md
@test/integration/bootstrap-managed-repo.test.ts
@scripts/smoke/bootstrap-managed-repo.sh
@scripts/smoke/bootstrap-planning.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing managed-bootstrap coverage for Phase 4 execution</name>
  <files>test/integration/bootstrap-managed-repo.test.ts, test/integration/bootstrap-planning.test.ts, scripts/smoke/bootstrap-managed-repo.sh, scripts/smoke/bootstrap-planning.sh</files>
  <read_first>src/cli/commands/bootstrap.ts, src/cli/resume/run.ts</read_first>
  <action>Update the existing managed-bootstrap integration and smoke coverage so they stub the new execution runner and assert the new execution artifacts rather than accidentally invoking the real local Codex CLI during tests. Keep the prior planning assertions, but extend them to confirm the runner output or execution summary exists when yolo auto-start is expected.</action>
  <verify>`bun test test/integration/bootstrap-managed-repo.test.ts` and `bun test test/integration/bootstrap-planning.test.ts`</verify>
  <acceptance_criteria>
    - existing yolo bootstrap coverage uses a deterministic execution-runner stub
    - tests assert execution artifacts in addition to planning artifacts
  </acceptance_criteria>
  <done>Existing bootstrap coverage remains trustworthy after Phase 4 changes the live yolo behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke coverage for yolo execution and resume recovery</name>
  <files>scripts/smoke/bootstrap-execution.sh, scripts/smoke/resume-execution.sh</files>
  <read_first>scripts/smoke/bootstrap-managed-repo.sh, scripts/lib/run-logging.sh</read_first>
  <action>Create smoke scripts that prove the new execution layer behaves correctly in real CLI flows. One should run yolo bootstrap with a stubbed execution runner and verify the execution-state, events, and summary artifacts. The other should simulate a failed runner, then use `yolo-port resume` with a succeeding stub to prove the recovery path. Keep them breadcrumb-heavy and store their outputs under `.codex/run-logs/` like the existing smoke suite.</action>
  <verify>`bash scripts/smoke/bootstrap-execution.sh` and `bash scripts/smoke/resume-execution.sh`</verify>
  <acceptance_criteria>
    - bootstrap execution smoke verifies execution-state and event artifacts
    - resume smoke verifies failed-run recovery through the explicit command
  </acceptance_criteria>
  <done>The execution/recovery loop has real shell-level proof, not just unit coverage.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test`
- [ ] `bash scripts/smoke/bootstrap-managed-repo.sh`
- [ ] `bash scripts/smoke/bootstrap-planning.sh`
- [ ] `bash scripts/smoke/bootstrap-execution.sh`
- [ ] `bash scripts/smoke/resume-execution.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Yolo execution and resume recovery both have end-to-end proof
- Existing bootstrap tests stay green after the new execution behavior lands
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-execution-and-recovery/04-03-SUMMARY.md`
</output>
