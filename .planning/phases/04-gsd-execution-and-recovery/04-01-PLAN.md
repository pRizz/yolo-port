---
phase: 04-gsd-execution-and-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/persistence/executionState.ts", "src/adapters/fs/executionState.ts", "src/domain/execution/handoff.ts", "src/adapters/system/gsd.ts", "src/ui/execution.ts", "test/fs/executionState.test.ts", "test/system/gsd.test.ts"]
autonomous: true
requirements: ["EXEC-02", "EXEC-03"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260412T012001Z
generated_at: 2026-04-12T01:20:01.688Z
must_haves:
  truths:
    - "yolo-port can build a concrete managed-execution handoff contract from the approved Phase 3 artifacts."
    - "Every major orchestration step has write-ahead and write-after event recording under .planning/yolo-port."
    - "The default runner can invoke Codex non-interactively, while tests can override the runner boundary deterministically."
  artifacts:
    - path: "src/persistence/executionState.ts"
      provides: "versioned execution state, event, and handoff schemas"
      exports: ["createManagedExecutionStateRecord", "createManagedExecutionEventRecord"]
    - path: "src/adapters/fs/executionState.ts"
      provides: "filesystem boundary for execution state and append-only events"
      exports: ["readManagedExecutionState", "writeManagedExecutionState", "appendManagedExecutionEvent"]
    - path: "src/domain/execution/handoff.ts"
      provides: "Codex/GSD handoff prompt generation from saved planning artifacts"
      exports: ["buildManagedExecutionHandoff"]
    - path: "src/adapters/system/gsd.ts"
      provides: "managed execution runner boundary"
      exports: ["runManagedExecution"]
  key_links:
    - from: "src/domain/execution/handoff.ts"
      to: "src/adapters/system/gsd.ts"
      via: "generated prompt and runner invocation contract"
      pattern: "runManagedExecution"
    - from: "src/adapters/fs/executionState.ts"
      to: "src/persistence/executionState.ts"
      via: "state and event schema persistence"
      pattern: "ManagedExecutionStateRecord|ManagedExecutionEventRecord"
---

<objective>
Create the managed execution contract and checkpoint persistence layer that Phase 4 will orchestrate.

Purpose: Before yolo-port can auto-start or resume execution, it needs a durable execution-state model, a prompt/contract for the external runner, and a safe system boundary for Codex-first invocation.
Output: Execution-state schemas, append-only event persistence, a generated handoff prompt/contract, and a pluggable runner adapter.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gsd-execution-and-recovery/04-CONTEXT.md
@.planning/phases/04-gsd-execution-and-recovery/04-RESEARCH.md
@src/adapters/system/gsd.ts
@src/adapters/fs/portPlanning.ts
@src/persistence/portPlanning.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the execution state, event, and handoff schemas</name>
  <files>src/persistence/executionState.ts, src/adapters/fs/executionState.ts, test/fs/executionState.test.ts</files>
  <read_first>src/persistence/portPlanning.ts, src/adapters/fs/portPlanning.ts</read_first>
  <action>Define the versioned managed-execution state machine records: current status, current step, completed steps, resume command, last error, and generated file paths. Add append-only event records that support write-ahead and write-after logging for each orchestration step. Implement the filesystem boundary that reads and writes `execution-state.json`, appends to `execution-events.jsonl`, and persists handoff/output/summary paths under `.planning/yolo-port/`. Keep writes atomic and make the tests prove append-only behavior plus deterministic state reads.</action>
  <verify>`bun test test/fs/executionState.test.ts`</verify>
  <acceptance_criteria>
    - `execution-state.json` and `execution-events.jsonl` are stored under `.planning/yolo-port/`
    - event appends preserve previous entries and state writes are atomic
  </acceptance_criteria>
  <done>The execution-state persistence layer can support auto-start, failure recovery, and resume without guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the managed execution handoff contract and Codex-first runner boundary</name>
  <files>src/domain/execution/handoff.ts, src/adapters/system/gsd.ts, src/ui/execution.ts, test/system/gsd.test.ts</files>
  <read_first>src/cli/bootstrap/planning.ts, .planning/phases/03-parity-planning-and-estimation/03-CONTEXT.md</read_first>
  <action>Build the prompt/contract generator that turns the approved Phase 3 artifacts into a concrete managed-execution handoff. The output must point at the saved planning artifacts, reiterate the 1:1 parity expectation, and name the resume/summary artifacts yolo-port owns. Extend the GSD adapter boundary so it can invoke a configured execution runner from the environment for tests, or default to `codex exec` with a non-interactive Codex-first prompt. Add a concise UI renderer for execution-step narration and test that the adapter calls the configured runner deterministically.</action>
  <verify>`bun test test/system/gsd.test.ts`</verify>
  <acceptance_criteria>
    - the handoff prompt names the saved planning artifacts and parity expectation
    - the GSD adapter can run a stub executor through an environment override
  </acceptance_criteria>
  <done>Phase 4 has a real execution boundary instead of a placeholder command suggestion.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/fs/executionState.test.ts`
- [ ] `bun test test/system/gsd.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Managed execution has a durable contract and checkpoint persistence layer
- The default runner is Codex-first and overrideable for tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-execution-and-recovery/04-01-SUMMARY.md`
</output>
