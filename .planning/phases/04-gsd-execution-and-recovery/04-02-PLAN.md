---
phase: 04-gsd-execution-and-recovery
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: ["src/cli/resume/run.ts", "src/cli/commands/resume.ts", "src/cli/commands/bootstrap.ts", "src/cli/bootstrap/interaction.ts", "src/cli/router.ts", "src/ui/help.ts", "test/integration/resume-execution.test.ts"]
autonomous: true
requirements: ["EXEC-02", "EXEC-03", "EXEC-04", "EXEC-05"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260412T012001Z
generated_at: 2026-04-12T01:20:01.688Z
must_haves:
  truths:
    - "YOLO bootstrap can auto-start the managed execution without extra yolo-port prompts after planning approval."
    - "The explicit resume command can continue a ready or failed managed run from the last reliable checkpoint."
    - "Guided and standard flows can intentionally trigger execution later through the same shared orchestrator."
  artifacts:
    - path: "src/cli/resume/run.ts"
      provides: "shared managed-execution orchestrator for bootstrap and resume"
      exports: ["resumeManagedExecution"]
    - path: "src/cli/commands/resume.ts"
      provides: "real resume command"
      exports: ["createResumeCommand"]
    - path: "test/integration/resume-execution.test.ts"
      provides: "proof that failed runs can resume successfully"
      contains: "execution-state"
  key_links:
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/cli/resume/run.ts"
      via: "yolo auto-start after planning approval"
      pattern: "resumeManagedExecution"
    - from: "src/cli/commands/resume.ts"
      to: "src/cli/resume/run.ts"
      via: "explicit resume path"
      pattern: "resumeManagedExecution"
    - from: "src/cli/router.ts"
      to: "src/cli/commands/resume.ts"
      via: "visible resume command becomes executable"
      pattern: "createResumeCommand"
---

<objective>
Wire the managed execution orchestrator into bootstrap and make the explicit `resume` command real.

Purpose: Phase 4 is only complete if yolo mode can continue automatically and interrupted runs can be resumed intentionally later. This plan connects the checkpointed execution layer to the existing CLI surfaces.
Output: Shared managed-execution orchestration, real `resume` command support, and integration coverage for resume after failure.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gsd-execution-and-recovery/04-CONTEXT.md
@.planning/phases/04-gsd-execution-and-recovery/04-RESEARCH.md
@src/cli/commands/bootstrap.ts
@src/cli/router.ts
@src/ui/help.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared managed-execution orchestrator with step-level recovery</name>
  <files>src/cli/resume/run.ts, test/integration/resume-execution.test.ts</files>
  <read_first>src/adapters/fs/executionState.ts, src/domain/execution/handoff.ts, .planning/research/PITFALLS.md</read_first>
  <action>Implement the orchestrator that loads or initializes execution state, chooses the next step from the last completed checkpoint, records write-ahead and write-after events, and runs the managed execution boundary. If the runner step fails, mark the state as failed and keep the last reliable checkpoint intact so a later resume re-runs only the unfinished step. Add an integration test that fails the stub runner on the first attempt, verifies the failed checkpointed state, then reruns `yolo-port resume` successfully with the same repo.</action>
  <verify>`bun test test/integration/resume-execution.test.ts`</verify>
  <acceptance_criteria>
    - resume starts from the first incomplete step instead of replaying finished steps
    - a failed runner leaves a resumable failed state and a second resume can complete
  </acceptance_criteria>
  <done>The managed execution state machine is deterministic and supports actual recovery rather than best-effort retry guesses.</done>
</task>

<task type="auto">
  <name>Task 2: Make bootstrap auto-start yolo execution and make `resume` real</name>
  <files>src/cli/commands/resume.ts, src/cli/commands/bootstrap.ts, src/cli/bootstrap/interaction.ts, src/cli/router.ts, src/ui/help.ts</files>
  <read_first>src/cli/commands/bootstrap.ts, src/ui/summary.ts</read_first>
  <action>Wire the new shared orchestrator into the CLI. Bootstrap should auto-start managed execution immediately when the mode is yolo and the Phase 3 plan is approved. Guided and standard flows should persist the ready state, update the completion summary honestly, and point users at `yolo-port resume`. Replace the placeholder resume command with the real command implementation and update the help text/examples to reflect the new flow.</action>
  <verify>`bun test test/integration/resume-execution.test.ts`</verify>
  <acceptance_criteria>
    - yolo bootstrap starts execution automatically after the plan is approved
    - `resume` is no longer a placeholder and appears in help as a working recovery command
  </acceptance_criteria>
  <done>Phase 4 becomes reachable from the real CLI surfaces instead of remaining a hidden adapter feature.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/integration/resume-execution.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- YOLO auto-start works
- Resume is explicit, real, and checkpoint-aware
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-execution-and-recovery/04-02-SUMMARY.md`
</output>
