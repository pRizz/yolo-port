---
phase: 03-parity-planning-and-estimation
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified: ["src/domain/estimates/planEstimate.ts", "src/cli/commands/bootstrap.ts", "src/cli/bootstrap/interaction.ts", "src/cli/bootstrap/planning.ts", "src/ui/summary.ts", "test/domain/estimates/planEstimate.test.ts", "test/integration/bootstrap-managed-repo.test.ts"]
autonomous: true
requirements: ["PLAN-03", "PLAN-04"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260411T221530Z
generated_at: 2026-04-11T22:15:30.848Z
must_haves:
  truths:
    - "The user can see duration, token, and USD estimate ranges tied to the selected model path."
    - "The planning preview exposes the pricing snapshot date and source provenance used for the estimate."
    - "Guided and standard flows can save an explicit proceed decision, while yolo and assume-yes auto-approve."
  artifacts:
    - path: "src/domain/estimates/planEstimate.ts"
      provides: "Repository-complexity estimate range calculation"
      exports: ["buildPortPlanEstimate"]
    - path: "src/cli/bootstrap/planning.ts"
      provides: "Phase 3 bootstrap-planning orchestration and proceed-decision persistence"
      contains: "buildPortPlanEstimate"
    - path: "test/domain/estimates/planEstimate.test.ts"
      provides: "Proof for estimate ranges and USD calculation"
      contains: "usdRange"
    - path: "test/integration/bootstrap-managed-repo.test.ts"
      provides: "Managed bootstrap proof including Phase 3 planning artifacts"
      contains: "port-plan"
  key_links:
    - from: "src/cli/bootstrap/planning.ts"
      to: "src/domain/estimates/planEstimate.ts"
      via: "estimate generation from repo stats, inventory, and pricing snapshot"
      pattern: "buildPortPlanEstimate"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/cli/bootstrap/interaction.ts"
      via: "saved proceed gate after planning preview"
      pattern: "confirmPlanningApproval"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/ui/summary.ts"
      via: "complete summary includes Phase 3 planning artifacts"
      pattern: "renderBootstrapSummary"
---

<objective>
Generate the estimate ranges and user-facing proceed gate, then persist the final Phase 3 planning artifacts during bootstrap completion.

Purpose: Phase 3 finishes when the user can see a parity-first execution plan with credible ranges and saved intent for downstream execution. This plan ties together the source reference, interface inventory, and pricing catalog into one preview and approval flow.
Output: Estimate range calculation, proceed-gate prompting, persisted Phase 3 planning artifacts, and updated bootstrap integration coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-parity-planning-and-estimation/03-CONTEXT.md
@.planning/phases/03-parity-planning-and-estimation/03-RESEARCH.md
@src/cli/commands/bootstrap.ts
@src/cli/bootstrap/execute.ts
@src/ui/summary.ts
@src/ui/planning.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the estimate range engine from repo complexity and pricing selection</name>
  <files>src/domain/estimates/planEstimate.ts, test/domain/estimates/planEstimate.test.ts</files>
  <read_first>src/domain/estimates/pricing.ts, src/domain/parity/inventory.ts</read_first>
  <action>Implement the pure estimate engine that translates repo stats, detected interface count, selected provider/model, and reasoning profile into duration, token, and USD ranges plus confidence and assumptions. Keep the math heuristic and transparent rather than pretending to be exact. Use the provider pricing entry for cost calculation, include cached-input pricing when present, and make the test prove that the output names the selected model path and snapshot provenance.</action>
  <verify>`bun test test/domain/estimates/planEstimate.test.ts`</verify>
  <acceptance_criteria>
    - estimate output contains duration, token, and USD ranges
    - cost calculation uses the selected pricing entry and snapshot metadata
  </acceptance_criteria>
  <done>The estimate layer is pure, explainable, and tied directly to the selected provider/model path.</done>
</task>

<task type="auto">
  <name>Task 2: Persist the planning preview and save a proceed decision during bootstrap completion</name>
  <files>src/cli/bootstrap/interaction.ts, src/cli/bootstrap/planning.ts, src/cli/commands/bootstrap.ts, src/ui/summary.ts, test/integration/bootstrap-managed-repo.test.ts</files>
  <read_first>src/cli/commands/bootstrap.ts, src/cli/bootstrap/interaction.ts, src/ui/bootstrap.ts</read_first>
  <action>Wire the new planning-preview pipeline into the bootstrap command after live execution succeeds. Render a dedicated Phase 3 planning preview section, then save a proceed decision: yolo or `--yes` flows auto-approve, while guided and standard flows ask explicitly and persist the answer. Update the completion summary so the user can see which planning artifacts were written and what the next honest action is, and extend the managed bootstrap integration test to assert the new artifacts exist.</action>
  <verify>`bun test test/integration/bootstrap-managed-repo.test.ts`</verify>
  <acceptance_criteria>
    - live bootstrap renders a plan preview before the final complete banner
    - proceed decisions are persisted instead of kept only in terminal output
  </acceptance_criteria>
  <done>Phase 3 finishes inside the existing bootstrap flow with a stored estimate package and saved proceed state.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/domain/estimates/planEstimate.test.ts`
- [ ] `bun test test/integration/bootstrap-managed-repo.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Estimate output includes selected model path, pricing snapshot date, and official provenance
- Proceed decisions are explicit and persisted for downstream phases
- Bootstrap completion stays honest about Phase 4 owning the real execution handoff
</success_criteria>

<output>
After completion, create `.planning/phases/03-parity-planning-and-estimation/03-04-SUMMARY.md`
</output>
