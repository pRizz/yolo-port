---
phase: 03-parity-planning-and-estimation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/adapters/system/git.ts", "src/persistence/portPlanning.ts", "src/adapters/fs/portPlanning.ts", "src/cli/bootstrap/planning.ts", "test/fs/portPlanning.test.ts"]
autonomous: true
requirements: ["REPO-03"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260411T221530Z
generated_at: 2026-04-11T22:15:30.848Z
must_haves:
  truths:
    - "Every managed repo gets a durable source reference before later execution phases mutate structure."
    - "Git-backed repos preserve a stable reference tag or equivalent git metadata at the pre-execution HEAD."
    - "Structural intent is persisted alongside the source reference so later phases know the target stack and parity goal."
  artifacts:
    - path: "src/adapters/system/git.ts"
      provides: "Git metadata and source-reference preservation helpers"
      exports: ["readGitSourceReference", "preserveGitSourceReference"]
    - path: "src/adapters/fs/portPlanning.ts"
      provides: "Managed artifact writer for Phase 3 planning outputs"
      exports: ["writePortPlanningArtifacts"]
    - path: "src/persistence/portPlanning.ts"
      provides: "Versioned schemas for source references and Phase 3 planning artifacts"
      exports: ["createSourceReferenceRecord"]
    - path: "test/fs/portPlanning.test.ts"
      provides: "Filesystem proof for source-reference artifact persistence"
      contains: "source-reference"
  key_links:
    - from: "src/cli/bootstrap/planning.ts"
      to: "src/adapters/system/git.ts"
      via: "source-reference preservation before planning preview generation"
      pattern: "preserveGitSourceReference|readGitSourceReference"
    - from: "src/cli/bootstrap/planning.ts"
      to: "src/adapters/fs/portPlanning.ts"
      via: "persist source reference and structural intent under .planning/yolo-port"
      pattern: "writePortPlanningArtifacts"
---

<objective>
Preserve the source implementation state before later phases attempt destructive work, and persist the structural intent that Phase 4 will consume.

Purpose: Phase 3 is not credible unless it can point to a stable reference for the original implementation. Source preservation must happen before interface inventory and estimate generation so later parity checks have a trustworthy baseline.
Output: Git-aware source-reference helpers, versioned source-reference persistence, and bootstrap-planning orchestration that records structural intent under `.planning/yolo-port/`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-parity-planning-and-estimation/03-CONTEXT.md
@.planning/phases/03-parity-planning-and-estimation/03-RESEARCH.md
@src/cli/bootstrap/execute.ts
@src/adapters/system/git.ts
@src/adapters/fs/planning.ts
@src/persistence/intakeProfile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add versioned source-reference persistence and artifact writing</name>
  <files>src/persistence/portPlanning.ts, src/adapters/fs/portPlanning.ts, test/fs/portPlanning.test.ts</files>
  <read_first>src/adapters/fs/planning.ts, src/persistence/bootstrapState.ts</read_first>
  <action>Define the versioned Phase 3 artifact schemas for source reference, interface inventory, pricing snapshot usage, estimate summary, and saved proceed decisions. Implement the `.planning/yolo-port/` writer for these artifacts so later plans can persist JSON and markdown without spreading path logic through the CLI. Keep the writer narrowly focused on machine-owned files and make the initial test prove that a source-reference artifact can be written deterministically.</action>
  <verify>`bun test test/fs/portPlanning.test.ts`</verify>
  <acceptance_criteria>
    - `src/persistence/portPlanning.ts` exports a source-reference factory with `schemaVersion`
    - `src/adapters/fs/portPlanning.ts` writes under `.planning/yolo-port/`
  </acceptance_criteria>
  <done>Phase 3 has one filesystem boundary for its new managed artifacts, and source-reference persistence is test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the git adapter to preserve a pre-execution source reference</name>
  <files>src/adapters/system/git.ts, src/cli/bootstrap/planning.ts</files>
  <read_first>src/adapters/system/git.ts, src/cli/bootstrap/target.ts</read_first>
  <action>Extend the git adapter with helpers that read branch, HEAD sha, and remotes, then preserve a stable tag such as `yolo-port/source-reference` when the repo is in git. Return a complete source-reference record for both git-backed and non-git repos so the bootstrap-planning step can persist structural intent without branching on CLI details. The planner should treat gitless repos as supported with a manifest-style fallback rather than as hard failures.</action>
  <verify>`bun test test/fs/portPlanning.test.ts`</verify>
  <acceptance_criteria>
    - `src/adapters/system/git.ts` exposes explicit source-reference helpers
    - bootstrap-planning orchestration can request a source reference without shelling out directly
  </acceptance_criteria>
  <done>Managed repos preserve a trustworthy source reference before parity scanning starts, regardless of whether git metadata is available.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/fs/portPlanning.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Source reference preservation happens before any parity or estimate artifact is persisted
- Git repos keep a stable pre-execution reference while non-git repos degrade gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/03-parity-planning-and-estimation/03-01-SUMMARY.md`
</output>
