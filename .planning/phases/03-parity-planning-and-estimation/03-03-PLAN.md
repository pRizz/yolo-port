---
phase: 03-parity-planning-and-estimation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: ["src/persistence/pricingCatalog.ts", "src/domain/estimates/pricing.ts", "test/domain/estimates/pricing.test.ts"]
autonomous: true
requirements: ["PLAN-03", "PLAN-04"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260411T221530Z
generated_at: 2026-04-11T22:15:30.848Z
must_haves:
  truths:
    - "Pricing snapshot data is versioned in-repo with captured dates and official source provenance."
    - "The selected estimate path resolves a provider/model pair from saved preferences plus the planning config profile."
    - "Estimate inputs remain transparent about pricing source and capture date."
  artifacts:
    - path: "src/persistence/pricingCatalog.ts"
      provides: "Versioned provider pricing snapshot catalog"
      exports: ["DEFAULT_PRICING_SNAPSHOT"]
    - path: "src/domain/estimates/pricing.ts"
      provides: "Provider alias and model-profile resolution logic"
      exports: ["resolveEstimateSelection"]
    - path: "test/domain/estimates/pricing.test.ts"
      provides: "Proof for provider/model selection and snapshot provenance"
      contains: "capturedAt"
  key_links:
    - from: "src/cli/bootstrap/planning.ts"
      to: "src/domain/estimates/pricing.ts"
      via: "selected provider/model resolution for estimate generation"
      pattern: "resolveEstimateSelection"
    - from: "src/domain/estimates/pricing.ts"
      to: "src/persistence/pricingCatalog.ts"
      via: "pricing entry lookup by provider and model profile"
      pattern: "DEFAULT_PRICING_SNAPSHOT"
---

<objective>
Build the versioned pricing snapshot and provider/model resolution layer that the estimate engine depends on.

Purpose: Cost estimates are only credible when they tie back to an explicit provider/model selection and an official captured pricing snapshot. This plan isolates those concerns before the estimate math lands.
Output: A dated provider pricing catalog, provider/model-profile selection logic, and unit tests that prove provenance and alias resolution.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-parity-planning-and-estimation/03-CONTEXT.md
@.planning/phases/03-parity-planning-and-estimation/03-RESEARCH.md
@.planning/config.json
@src/persistence/intakeProfile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the versioned provider pricing snapshot catalog</name>
  <files>src/persistence/pricingCatalog.ts</files>
  <read_first>.planning/research/STACK.md, .planning/research/PITFALLS.md</read_first>
  <action>Create the in-repo pricing snapshot catalog with a captured date, official source URLs, and provider-specific model entries. Include OpenAI and Anthropic models that cover the quality, balanced, and budget selection path, and store cached-input pricing separately when the provider publishes it. Keep the snapshot data explicit and machine-readable so the estimate output can cite provenance directly.</action>
  <verify>`bun test test/domain/estimates/pricing.test.ts`</verify>
  <acceptance_criteria>
    - `DEFAULT_PRICING_SNAPSHOT` includes source URLs and `capturedAt`
    - both OpenAI and Anthropic have model entries covering quality, balanced, and budget selections
  </acceptance_criteria>
  <done>Phase 3 has a stable, explicit pricing baseline that later execution and audit phases can reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve provider/model selection from saved preferences and config</name>
  <files>src/domain/estimates/pricing.ts, test/domain/estimates/pricing.test.ts</files>
  <read_first>src/domain/intake/preferences.ts, src/persistence/intakeProfile.ts</read_first>
  <action>Implement a pure selection function that maps the saved preferred agent/provider string and `.planning/config.json` model profile onto a provider, model, reasoning profile, and pricing entry from the snapshot catalog. Default unknown providers to the OpenAI path, keep `quality`, `balanced`, and `budget` explicit, and surface the reasoning-profile mapping in the returned value so the estimate output stays transparent.</action>
  <verify>`bun test test/domain/estimates/pricing.test.ts`</verify>
  <acceptance_criteria>
    - selection returns provider, model, reasoning profile, and pricing provenance
    - unknown or missing preference strings fall back predictably instead of throwing
  </acceptance_criteria>
  <done>The estimate engine can name a concrete provider/model path and the pricing snapshot that supports it.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/domain/estimates/pricing.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Pricing provenance is explicit and stored in-repo
- Selected model resolution is deterministic and tied to saved preferences plus config
</success_criteria>

<output>
After completion, create `.planning/phases/03-parity-planning-and-estimation/03-03-SUMMARY.md`
</output>
