---
phase: 03-parity-planning-and-estimation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["src/adapters/fs/repositorySnapshot.ts", "src/domain/parity/inventory.ts", "src/domain/parity/checklist.ts", "src/ui/planning.ts", "test/domain/parity/inventory.test.ts", "test/integration/bootstrap-planning.test.ts", "scripts/smoke/bootstrap-planning.sh"]
autonomous: true
requirements: ["PLAN-01", "PLAN-02"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260411T221530Z
generated_at: 2026-04-11T22:15:30.848Z
must_haves:
  truths:
    - "The user can review a high-level inventory of the repo's externally visible interfaces before execution."
    - "The stored parity checklist defaults detected surfaces to 1:1 parity and calls out the exception policy explicitly."
    - "Phase 3 inventory stays within the static-inspection safety rule and does not execute repo code."
  artifacts:
    - path: "src/adapters/fs/repositorySnapshot.ts"
      provides: "Safe repository file walking and text snapshotting for static analysis"
      exports: ["scanRepositorySnapshot"]
    - path: "src/domain/parity/inventory.ts"
      provides: "High-level interface inventory generation"
      exports: ["buildInterfaceInventory"]
    - path: "src/domain/parity/checklist.ts"
      provides: "Parity checklist generation from detected external surfaces"
      exports: ["buildParityChecklist"]
    - path: "test/domain/parity/inventory.test.ts"
      provides: "Proof that CLI, route, env, and config surfaces are detected from representative repos"
      contains: "/health"
  key_links:
    - from: "src/cli/bootstrap/planning.ts"
      to: "src/adapters/fs/repositorySnapshot.ts"
      via: "static repository scan for Phase 3 inventory generation"
      pattern: "scanRepositorySnapshot"
    - from: "src/cli/bootstrap/planning.ts"
      to: "src/domain/parity/inventory.ts"
      via: "detected interface inventory before estimate rendering"
      pattern: "buildInterfaceInventory"
    - from: "src/domain/parity/inventory.ts"
      to: "src/domain/parity/checklist.ts"
      via: "inventory-to-checklist translation"
      pattern: "buildParityChecklist"
---

<objective>
Generate a high-level inventory of externally visible repo surfaces and turn it into a parity checklist the user can review before execution starts.

Purpose: The parity promise is only real if the tool inventories what must stay compatible. This plan turns the Phase 3 source reference into actionable parity planning by scanning the repo statically and rendering the detected surface area.
Output: Static repo scanning, interface inventory generation, parity checklist rendering, and automated coverage for representative CLI and API surfaces.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-parity-planning-and-estimation/03-CONTEXT.md
@.planning/phases/03-parity-planning-and-estimation/03-RESEARCH.md
@src/cli/bootstrap/execute.ts
@src/adapters/fs/managedRepo.ts
@src/domain/intake/normalizeIntake.ts
@src/ui/summary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a safe repository snapshot adapter for static analysis</name>
  <files>src/adapters/fs/repositorySnapshot.ts</files>
  <read_first>src/adapters/fs/managedRepo.ts, src/adapters/system/git.ts</read_first>
  <action>Implement a repo snapshot adapter that walks the resolved repo root, skips heavyweight and generated directories such as `.git`, `node_modules`, `.planning`, `dist`, and `build`, and captures text content only for safe static-analysis extensions. The returned snapshot must include relative paths, line counts, language hints, and content for supported text files so domain scanners stay pure and never shell out or execute repo code.</action>
  <verify>`bun test test/domain/parity/inventory.test.ts`</verify>
  <acceptance_criteria>
    - `scanRepositorySnapshot` skips `.git`, `.planning`, and `node_modules`
    - snapshot entries include relative path, line count, and language hint
  </acceptance_criteria>
  <done>The parity inventory can rely on one safe, reusable snapshot of the repo rather than ad hoc filesystem reads.</done>
</task>

<task type="auto">
  <name>Task 2: Build the interface inventory and parity checklist domains</name>
  <files>src/domain/parity/inventory.ts, src/domain/parity/checklist.ts, test/domain/parity/inventory.test.ts</files>
  <read_first>src/domain/intake/types.ts, .planning/research/PITFALLS.md</read_first>
  <action>Define the high-level interface categories Phase 3 cares about and implement static detectors for CLI entrypoints and flags, HTTP routes, environment variables, config files, and public package/export surfaces where detectable. Translate the resulting inventory into a parity checklist that defaults each detected surface to required 1:1 parity and includes a clear exception policy instead of pretending divergence is normal. Keep the detectors heuristic but transparent, and cover representative repositories in the unit test.</action>
  <verify>`bun test test/domain/parity/inventory.test.ts`</verify>
  <acceptance_criteria>
    - the inventory model groups detected surfaces by category
    - the checklist output includes explicit 1:1 parity targets and an exception policy
  </acceptance_criteria>
  <done>The repo's public surface is summarized in a parity-focused format that downstream phases can reuse.</done>
</task>

<task type="auto">
  <name>Task 3: Render the planning preview through bootstrap and prove it end to end</name>
  <files>src/ui/planning.ts, src/cli/bootstrap/planning.ts, test/integration/bootstrap-planning.test.ts, scripts/smoke/bootstrap-planning.sh</files>
  <read_first>src/cli/commands/bootstrap.ts, src/ui/bootstrap.ts</read_first>
  <action>Render a Phase 3 planning preview section that summarizes the preserved source reference, the high-level interface inventory, and the parity checklist scope in concise terminal output. Wire the preview into the bootstrap-planning orchestration so live bootstrap runs surface this data before the final completion banner. Add integration and smoke coverage that bootstrap a representative repo and prove the stored parity checklist names the detected surfaces.</action>
  <verify>`bun test test/integration/bootstrap-planning.test.ts` and `bash scripts/smoke/bootstrap-planning.sh`</verify>
  <acceptance_criteria>
    - bootstrap output includes a dedicated planning preview section
    - the representative smoke repo yields detectable route, env, and CLI surfaces in the stored artifacts
  </acceptance_criteria>
  <done>Phase 3 inventory is user-visible during bootstrap and backed by automated end-to-end coverage.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/domain/parity/inventory.test.ts`
- [ ] `bun test test/integration/bootstrap-planning.test.ts`
- [ ] `bash scripts/smoke/bootstrap-planning.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Static inventory covers the agreed high-signal external surfaces
- The parity checklist is stored and rendered before later execution phases begin
</success_criteria>

<output>
After completion, create `.planning/phases/03-parity-planning-and-estimation/03-02-SUMMARY.md`
</output>
