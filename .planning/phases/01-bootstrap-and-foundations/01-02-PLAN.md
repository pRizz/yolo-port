---
phase: 01-bootstrap-and-foundations
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
objective: "Add the real tool-verification layer for Phase 1: Bun detection/install at the launcher boundary, plus Codex/GSD preflight and an explicit bootstrap action plan."
files_modified: ["bin/yolo-port.js", "bin/lib/ensure-bun.js", "src/cli/commands/bootstrap.ts", "src/cli/flags.ts", "src/domain/bootstrap/types.ts", "src/domain/bootstrap/planBootstrap.ts", "src/adapters/system/bun.ts", "src/adapters/system/gsd.ts", "src/ui/progress.ts", "src/ui/actionLog.ts", "test/domain/bootstrap/planBootstrap.test.ts", "test/system/bun.test.ts", "test/system/gsd.test.ts", "test/integration/bootstrap-bun-install.test.ts", "scripts/smoke/bootstrap-tools.sh"]
files-modified: ["bin/yolo-port.js", "bin/lib/ensure-bun.js", "src/cli/commands/bootstrap.ts", "src/cli/flags.ts", "src/domain/bootstrap/types.ts", "src/domain/bootstrap/planBootstrap.ts", "src/adapters/system/bun.ts", "src/adapters/system/gsd.ts", "src/ui/progress.ts", "src/ui/actionLog.ts", "test/domain/bootstrap/planBootstrap.test.ts", "test/system/bun.test.ts", "test/system/gsd.test.ts", "test/integration/bootstrap-bun-install.test.ts", "scripts/smoke/bootstrap-tools.sh"]
autonomous: true
requirements: ["BOOT-02", "BOOT-03"]
user_setup: []
must_haves:
  truths:
    - "Bootstrap follows the agreed first-run order: checks, questions, summary, then execute."
    - "If Bun is missing, the CLI can offer install-and-continue before any Bun-dependent step runs."
    - "The CLI can determine and explain the correct Codex-facing GSD action without requiring the user to understand GSD internals."
    - "YOLO mode still prints automatic tool actions and reasons before executing them."
  artifacts:
    - path: "bin/lib/ensure-bun.js"
      provides: "Node-compatible Bun detect/install/re-detect helper"
      exports: ["ensureBun"]
    - path: "src/domain/bootstrap/planBootstrap.ts"
      provides: "Pure bootstrap action planner"
      exports: ["planBootstrap"]
    - path: "src/adapters/system/bun.ts"
      provides: "In-Bun Bun state and version adapter"
      exports: ["detectBun", "readBunVersion"]
    - path: "src/adapters/system/gsd.ts"
      provides: "Codex-first GSD preflight and deferred action planner"
      exports: ["detectGsd", "planGsdAction"]
    - path: "test/integration/bootstrap-bun-install.test.ts"
      provides: "End-to-end Bun-missing install-and-continue proof"
      contains: "PATH"
    - path: "scripts/smoke/bootstrap-tools.sh"
      provides: "Tool-flow smoke coverage with fixture PATHs"
      contains: "PATH="
  key_links:
    - from: "bin/yolo-port.js"
      to: "bin/lib/ensure-bun.js"
      via: "pre-Bun launcher bridge"
      pattern: "ensure-bun"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/domain/bootstrap/planBootstrap.ts"
      via: "checks/questions/summary/execute plan generation"
      pattern: "planBootstrap"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/adapters/system/gsd.ts"
      via: "Codex GSD preflight and deferred action planning"
      pattern: "planGsdAction"
---

<objective>
Add the real tool-verification layer for Phase 1: Bun detection/install at the launcher boundary, plus Codex/GSD preflight and an explicit bootstrap action plan.

Purpose: The CLI shell from 01-01 must now become a guided bootstrap flow that can explain what it will do before it mutates anything.
Output: A pure bootstrap planner, Node-compatible Bun install bridge, Codex/GSD detection and runner boundary, progress rendering, and smoke coverage for missing/present tool states.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-and-foundations/01-CONTEXT.md
@.planning/phases/01-bootstrap-and-foundations/01-RESEARCH.md
@.planning/phases/01-bootstrap-and-foundations/01-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Model bootstrap decisions as a pure checks-to-execute plan</name>
  <files>src/cli/flags.ts, src/domain/bootstrap/types.ts, src/domain/bootstrap/planBootstrap.ts, test/domain/bootstrap/planBootstrap.test.ts</files>
  <action>Define the domain model for Phase 1 bootstrap state and user intent, including mode selection (`guided`, `standard`, `yolo`), verbosity, detected tool state, and answers gathered during onboarding. Implement a pure planner that always emits the same ordered flow: checks, questions, summary, execute. Automatic actions must carry a reason string so the UI can explain them. Keep shelling out and filesystem mutation out of this layer so the decision logic stays unit-testable and reusable as Bright Builds and scaffold work land in 01-03.</action>
  <verify>`bun test test/domain/bootstrap/planBootstrap.test.ts`</verify>
  <done>The planner produces deterministic step/action sequences for missing Bun, present Bun, stale or unknown GSD, guided mode, and YOLO mode without depending on process execution.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Replace the Bun-missing dead end with install-and-continue behavior</name>
  <files>bin/yolo-port.js, bin/lib/ensure-bun.js, src/adapters/system/bun.ts, test/system/bun.test.ts, test/integration/bootstrap-bun-install.test.ts</files>
  <action>Extract a Node-compatible Bun bootstrap helper that the launcher can run before any Bun-dependent code executes. It must detect Bun on PATH, offer the officially supported install path when missing, re-detect Bun after installation, and only then hand off into the Bun entrypoint. Keep `--help` and `--version` Node-native, and use structured child-process execution instead of raw shell interpolation so repo paths and installer arguments stay safe and debuggable. Add an integration test that clears Bun from `PATH`, accepts the install path through a stubbed installer, proves Bun is re-detected, and confirms the launcher continues into a Bun-managed command such as `bootstrap --dry-run` rather than stopping at help output.</action>
  <verify>`node bin/yolo-port.js --help` proves the native help path stays Bun-free, and `bun test test/system/bun.test.ts test/integration/bootstrap-bun-install.test.ts` proves a Bun-missing machine can install Bun and continue into a non-help Bun-managed command.</verify>
  <done>The launcher no longer fails hard on Bun-less machines: it can either render native help or complete a tested install-and-continue path into a real Bun-managed command.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add Codex/GSD preflight, progress output, and smoke coverage</name>
  <files>src/cli/commands/bootstrap.ts, src/adapters/system/gsd.ts, src/ui/progress.ts, src/ui/actionLog.ts, test/system/gsd.test.ts, scripts/smoke/bootstrap-tools.sh</files>
  <action>Implement the Phase 1 preflight for Codex/GSD around a single adapter boundary. Detect whether repo-local Codex/GSD assets are present, stale, unknown, or missing; plan the correct action; and expose only the preflight and action-planning surface in this wave. Do not let multiple modules shell out to GSD independently. Surface the planned action through section banners, concise action logs, and reasons for each automatic step, and keep YOLO mode visible rather than opaque. Because Bright Builds must gate repo-local writes, explicitly defer any repo-local GSD mutation or install execution until 01-03 after the standards check is clear, and prove the 01-02 behavior with stubbed planners and PATH-based smoke tests.</action>
  <verify>`bun test test/system/gsd.test.ts` and `bash scripts/smoke/bootstrap-tools.sh`</verify>
  <done>The CLI can preflight Bun and Codex/GSD cleanly, explain exactly what it intends to do, and prove through tests and smoke scripts that missing, present, and stale tool states behave deterministically without performing repo-local GSD writes before the Bright Builds gate exists.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test`
- [ ] `bash scripts/smoke/bootstrap-tools.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Bun installation is offered before any Bun-only step runs
- Bootstrap renders checks, questions, summary, and execute in that order
- Codex/GSD preflight is centralized behind one adapter and one action planner, with repo-local mutation deferred to 01-03
- YOLO mode still prints planned automatic actions and their reasons
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-and-foundations/01-02-SUMMARY.md`
</output>
