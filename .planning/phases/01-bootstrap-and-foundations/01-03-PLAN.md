---
phase: 01-bootstrap-and-foundations
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
objective: "Finish the Phase 1 bootstrap by integrating Bright Builds onboarding, safe repo-local GSD execution ordering, and automatic `.planning` scaffold creation for managed ports."
files_modified: ["src/cli/commands/bootstrap.ts", "src/adapters/system/brightBuilds.ts", "src/adapters/system/gsd.ts", "src/adapters/fs/planning.ts", "src/persistence/bootstrapState.ts", "src/ui/summary.ts", "src/ui/recovery.ts", "src/templates/planning/PROJECT.md.tpl", "src/templates/planning/ROADMAP.md.tpl", "src/templates/planning/STATE.md.tpl", "src/templates/planning/REQUIREMENTS.md.tpl", "src/templates/planning/config.json.tpl", "src/templates/planning/yolo-port/manifest.json.tpl", "src/templates/planning/yolo-port/bootstrap-state.json.tpl", "test/system/brightBuilds.test.ts", "test/fs/planning.test.ts", "test/integration/bootstrap-managed-repo.test.ts", "scripts/smoke/bootstrap-managed-repo.sh"]
files-modified: ["src/cli/commands/bootstrap.ts", "src/adapters/system/brightBuilds.ts", "src/adapters/system/gsd.ts", "src/adapters/fs/planning.ts", "src/persistence/bootstrapState.ts", "src/ui/summary.ts", "src/ui/recovery.ts", "src/templates/planning/PROJECT.md.tpl", "src/templates/planning/ROADMAP.md.tpl", "src/templates/planning/STATE.md.tpl", "src/templates/planning/REQUIREMENTS.md.tpl", "src/templates/planning/config.json.tpl", "src/templates/planning/yolo-port/manifest.json.tpl", "src/templates/planning/yolo-port/bootstrap-state.json.tpl", "test/system/brightBuilds.test.ts", "test/fs/planning.test.ts", "test/integration/bootstrap-managed-repo.test.ts", "scripts/smoke/bootstrap-managed-repo.sh"]
autonomous: true
requirements: ["BOOT-03", "BOOT-04", "EXEC-01"]
user_setup: []
must_haves:
  truths:
    - "Bootstrap can apply Bright Builds standards during onboarding and stops with a clear blocked explanation before repo-local writes when the repo is blocked."
    - "A successful bootstrap creates the managed `.planning` scaffold automatically, including `.planning/yolo-port/` as the machine-readable marker."
    - "Codex-facing GSD setup runs in the correct order relative to the Bright Builds gate and scaffold creation."
    - "The success summary reports installed or verified tools, files written, selected mode, warnings, repo status, and the exact next command."
  artifacts:
    - path: "src/adapters/system/brightBuilds.ts"
      provides: "Bright Builds status/install/update/force wrapper"
      exports: ["readBrightBuildsStatus", "runBrightBuildsAction"]
    - path: "src/adapters/fs/planning.ts"
      provides: "Idempotent `.planning` scaffold writer"
      contains: ".planning/yolo-port"
    - path: "src/persistence/bootstrapState.ts"
      provides: "Durable bootstrap state boundary for managed repos"
      contains: "schemaVersion"
    - path: "src/ui/summary.ts"
      provides: "Bootstrap success summary and next-step output"
      min_lines: 40
    - path: "scripts/smoke/bootstrap-managed-repo.sh"
      provides: "End-to-end managed-repo bootstrap smoke coverage"
      contains: "blocked"
  key_links:
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/adapters/system/brightBuilds.ts"
      via: "status gate before any repo-local GSD or planning writes"
      pattern: "readBrightBuildsStatus|runBrightBuildsAction"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/adapters/system/gsd.ts"
      via: "Codex GSD execution after standards gate clears repo-local mutation"
      pattern: "runGsdAction"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/adapters/fs/planning.ts"
      via: "scaffold writer invoked after standards and GSD steps succeed"
      pattern: "writePlanningScaffold"
    - from: "src/adapters/fs/planning.ts"
      to: "src/persistence/bootstrapState.ts"
      via: "machine-state files under `.planning/yolo-port/`"
      pattern: "bootstrap-state|manifest"
---

<objective>
Finish the Phase 1 bootstrap by integrating Bright Builds onboarding, safe repo-local GSD execution ordering, and automatic `.planning` scaffold creation for managed ports.

Purpose: Phase 1 is only complete when the tool can bootstrap a managed repo end to end, not merely plan the actions.
Output: A Bright Builds adapter with blocked-state handling, an idempotent planning scaffold writer, a durable machine-state boundary under `.planning/yolo-port/`, and end-to-end bootstrap smoke coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-and-foundations/01-CONTEXT.md
@.planning/phases/01-bootstrap-and-foundations/01-RESEARCH.md
@.planning/phases/01-bootstrap-and-foundations/01-01-SUMMARY.md
@.planning/phases/01-bootstrap-and-foundations/01-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@coding-and-architecture-requirements.audit.md
@scripts/bright-builds-auto-update.sh
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement Bright Builds onboarding with an explicit blocked-state gate</name>
  <files>src/adapters/system/brightBuilds.ts, src/ui/recovery.ts, src/cli/commands/bootstrap.ts, test/system/brightBuilds.test.ts</files>
  <action>Wrap the official Bright Builds downstream workflow behind one adapter that always checks `status` before it considers `install`, `update`, or `force`. Parse at least `installable`, `installed`, and `blocked`, and surface blocked details in a form that is easy to hand to an AI agent. If the repo is blocked, stop before any repo-local GSD installation or planning scaffold writes happen. Support an explicit interactive or flag-driven force path, but print the reason for that path clearly and keep blind install behavior out of the bootstrap.</action>
  <verify>`bun test test/system/brightBuilds.test.ts`</verify>
  <done>Bright Builds onboarding is deterministic, blocked repos fail safely before local writes, and the force path is explicit rather than hidden.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Create the managed `.planning` scaffold and durable state spine</name>
  <files>src/adapters/fs/planning.ts, src/persistence/bootstrapState.ts, src/templates/planning/PROJECT.md.tpl, src/templates/planning/ROADMAP.md.tpl, src/templates/planning/STATE.md.tpl, src/templates/planning/REQUIREMENTS.md.tpl, src/templates/planning/config.json.tpl, src/templates/planning/yolo-port/manifest.json.tpl, src/templates/planning/yolo-port/bootstrap-state.json.tpl, test/fs/planning.test.ts</files>
  <action>Implement conservative scaffold writing for a managed port repo. Create the human-facing `.planning` files only when absent or when a narrow merge is safe, and keep existing authored docs intact. Write the machine-owned state under `.planning/yolo-port/`, including the managed marker, schema version, selected mode, executed steps, and written-artifact records. Reserve the future persistence boundary now so Phase 4 can grow into checkpoint logging without rethinking the root layout. Write `.planning/yolo-port/` last among bootstrap-created artifacts so a standards block never leaves a false managed marker behind.</action>
  <verify>`bun test test/fs/planning.test.ts`</verify>
  <done>A successful scaffold run creates the required `.planning` baseline idempotently, preserves existing docs, and establishes `.planning/yolo-port/` as the only machine-readable managed marker.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Wire the final execute order, success summary, and managed-repo smoke test</name>
  <files>src/cli/commands/bootstrap.ts, src/adapters/system/gsd.ts, src/ui/summary.ts, test/integration/bootstrap-managed-repo.test.ts, scripts/smoke/bootstrap-managed-repo.sh</files>
  <action>Finish the real Phase 1 bootstrap orchestration. The user-facing flow must remain checks, questions, summary, then execute; inside execute, the ordered repo-local work must respect the research constraints: Bright Builds gate first, then the deferred Codex/GSD mutation runner from 01-02 only after the standards gate is clear, and planning scaffold creation last so `.planning/yolo-port/` is only written on a real success path. End each major step with next-step guidance where useful. The final summary must list installed or verified tools, files written, selected mode, warnings, repo status, the exact follow-up command, and next steps. Add an integration test and temp-repo smoke script that cover installable, already-installed, blocked, and forced paths, including rerun idempotence.</action>
  <verify>`bun test test/integration/bootstrap-managed-repo.test.ts` and `bash scripts/smoke/bootstrap-managed-repo.sh`</verify>
  <done>Phase 1 can bootstrap a managed repo end to end, blocked states halt safely, successful runs create the managed scaffold, and reruns do not duplicate or trample existing planning assets.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test`
- [ ] `bash scripts/smoke/bootstrap-managed-repo.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Bright Builds status is always evaluated before repo-local GSD or planning writes
- Codex-facing GSD setup runs without requiring the user to understand GSD internals
- `.planning` and `.planning/yolo-port/` are created automatically on successful bootstrap
- Bootstrap summaries include tools, files, mode, warnings, repo status, exact next command, and next steps
- Rerunning bootstrap is idempotent for already-managed repos
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-and-foundations/01-03-SUMMARY.md`
</output>
