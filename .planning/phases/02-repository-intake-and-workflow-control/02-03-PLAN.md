---
phase: 02-repository-intake-and-workflow-control
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
objective: "Persist workflow mode and optional taste-question responses in `.planning/yolo-port/` so reruns can reuse the full intake profile with clear summaries, override precedence, and yolo escalation."
files_modified: ["src/cli/commands/bootstrap.ts", "src/domain/intake/types.ts", "src/domain/intake/preferences.ts", "src/adapters/fs/intakeProfile.ts", "src/adapters/fs/planning.ts", "src/persistence/intakeProfile.ts", "src/ui/help.ts", "src/ui/summary.ts", "src/templates/planning/yolo-port/intake-profile.json.tpl", "test/domain/intake/preferences.test.ts", "test/fs/intakeProfile.test.ts", "test/integration/intake-preferences.test.ts", "scripts/smoke/intake-preferences.sh"]
files-modified: ["src/cli/commands/bootstrap.ts", "src/domain/intake/types.ts", "src/domain/intake/preferences.ts", "src/adapters/fs/intakeProfile.ts", "src/adapters/fs/planning.ts", "src/persistence/intakeProfile.ts", "src/ui/help.ts", "src/ui/summary.ts", "src/templates/planning/yolo-port/intake-profile.json.tpl", "test/domain/intake/preferences.test.ts", "test/fs/intakeProfile.test.ts", "test/integration/intake-preferences.test.ts", "scripts/smoke/intake-preferences.sh"]
autonomous: true
requirements: ["FLOW-01", "FLOW-02", "FLOW-03"]
user_setup: []
must_haves:
  truths:
    - "Users can choose guided, standard, or yolo involvement during intake, and the selected mode persists for reruns."
    - "Saved intake preferences are reused by default through a summary-first flow, not as a silent lock-in."
    - "Optional taste answers can be skipped initially and inferred later using Bright Builds-aligned defaults until they become relevant."
    - "Current CLI flags override current answers, and current answers override saved metadata; non-yolo flows can escalate to yolo with confirmation."
  artifacts:
    - path: "src/domain/intake/preferences.ts"
      provides: "Pure preference merge and taste-default inference logic"
      exports: ["mergeIntakePreferences", "inferTasteDefaults"]
    - path: "src/persistence/intakeProfile.ts"
      provides: "Versioned intake profile schema"
      exports: ["createIntakeProfileRecord"]
    - path: "src/adapters/fs/intakeProfile.ts"
      provides: "Machine-state read and write boundary for saved intake preferences"
      exports: ["readIntakeProfile", "writeIntakeProfile"]
    - path: "src/templates/planning/yolo-port/intake-profile.json.tpl"
      provides: "Scaffolded intake profile file under `.planning/yolo-port/`"
      contains: "\"schemaVersion\""
    - path: "test/integration/intake-preferences.test.ts"
      provides: "End-to-end proof for saved-preference reuse and override precedence"
      contains: "flags"
  key_links:
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/adapters/fs/intakeProfile.ts"
      via: "saved intake profile load and persistence after user decisions"
      pattern: "readIntakeProfile|writeIntakeProfile"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/domain/intake/preferences.ts"
      via: "flags, current answers, and saved metadata precedence"
      pattern: "mergeIntakePreferences|inferTasteDefaults"
    - from: "src/adapters/fs/planning.ts"
      to: "src/templates/planning/yolo-port/intake-profile.json.tpl"
      via: "managed scaffold adds the intake-profile slot"
      pattern: "intake-profile"
---

<objective>
Persist workflow mode and optional taste-question responses in `.planning/yolo-port/` so reruns can reuse the full intake profile with clear summaries, override precedence, and yolo escalation.

Purpose: Repository intake becomes much more valuable once it remembers what the user already told it. Phase 2 finishes by turning mode selection and taste answers into durable machine state that can guide reruns without trapping the user in stale settings.
Output: A versioned intake-profile schema, preference merge rules, machine-state read/write adapters, rerun summaries, and smoke coverage for reuse, overrides, and yolo escalation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-repository-intake-and-workflow-control/02-CONTEXT.md
@.planning/phases/02-repository-intake-and-workflow-control/02-RESEARCH.md
@.planning/phases/01-bootstrap-and-foundations/01-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define the persisted intake profile schema, override precedence, and taste defaults</name>
  <files>src/domain/intake/types.ts, src/domain/intake/preferences.ts, src/persistence/intakeProfile.ts, test/domain/intake/preferences.test.ts</files>
  <action>Model the full Phase 2 intake profile as versioned machine state: selected mode, source repo, target stack, clone destination, preferred agent or provider, whether the user wants taste questions, and the answers they actually supplied. Implement pure merge logic that applies the required precedence order of current CLI flags, then current interactive answers, then saved metadata. Add taste-default inference in the same spirit as the installed Bright Builds standards so skipped answers do not force extra prompts before they are relevant. Cover guided, standard, and yolo mode transitions, including escalation from non-yolo to yolo with confirmation.</action>
  <verify>`bun test test/domain/intake/preferences.test.ts`</verify>
  <done>The intake profile shape is explicit, merge precedence is deterministic, and deferred taste answers can be inferred without losing the ability to ask later.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add `.planning/yolo-port/` profile persistence and scaffold support</name>
  <files>src/adapters/fs/intakeProfile.ts, src/adapters/fs/planning.ts, src/templates/planning/yolo-port/intake-profile.json.tpl, test/fs/intakeProfile.test.ts</files>
  <action>Create the read and write boundary for `intake-profile.json` under `.planning/yolo-port/`. It should preserve versioning, write atomically, and stay idempotent on reruns. Extend the scaffold writer so new managed repos receive the intake-profile slot without disrupting existing Phase 1 behavior, and backfill or create the profile lazily when older bootstrapped repos first use Phase 2 features. Keep all of this state machine-owned and separate from the human-authored planning docs.</action>
  <verify>`bun test test/fs/intakeProfile.test.ts`</verify>
  <done>Managed repos have a stable place for saved intake preferences, and older repos can adopt it without a one-off migration script.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Reuse saved preferences in the intake UX and prove rerun behavior</name>
  <files>src/cli/commands/bootstrap.ts, src/ui/help.ts, src/ui/summary.ts, test/integration/intake-preferences.test.ts, scripts/smoke/intake-preferences.sh</files>
  <action>Integrate the saved intake profile into the intake flow. On reruns, show a summary of remembered mode, repo choices, and taste answers, then continue unless the user objects. Keep non-yolo flows informed that they can escalate to yolo, and require confirmation for the escalation and for other high-impact workspace-shaping actions agreed in context. If taste answers were skipped previously, infer sensible defaults for now and surface that they can be refined later. Add integration and smoke coverage that prove saved preferences are reused, flags and new answers override saved metadata, and mode selection persists cleanly across reruns.</action>
  <verify>`bun test test/integration/intake-preferences.test.ts` and `bash scripts/smoke/intake-preferences.sh`</verify>
  <done>Phase 2 finishes with a durable intake experience: mode and taste preferences persist, reruns are streamlined but transparent, and the current invocation always wins when it conflicts with saved state.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test`
- [ ] `bash scripts/smoke/intake-preferences.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Guided, standard, and yolo modes are persisted and reused across reruns
- Saved preferences are reused through a summary-first flow rather than a silent auto-lock
- Optional taste answers can be deferred and inferred with Bright Builds-aligned defaults
- Flags and fresh answers override saved metadata, and yolo escalation remains explicit
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-intake-and-workflow-control/02-03-SUMMARY.md`
</output>
