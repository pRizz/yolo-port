---
phase: 02-repository-intake-and-workflow-control
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
objective: "Build conservative repo-state classification and breadcrumb detection so intake can distinguish fresh, in-progress, and already ported repositories and present honest next actions."
files_modified: ["src/cli/commands/bootstrap.ts", "src/domain/intake/types.ts", "src/domain/intake/classifyRepoState.ts", "src/adapters/fs/managedRepo.ts", "src/persistence/managedRepoState.ts", "src/ui/classification.ts", "test/domain/intake/classifyRepoState.test.ts", "test/fs/managedRepo.test.ts", "test/integration/intake-classification.test.ts", "scripts/smoke/intake-classification.sh"]
files-modified: ["src/cli/commands/bootstrap.ts", "src/domain/intake/types.ts", "src/domain/intake/classifyRepoState.ts", "src/adapters/fs/managedRepo.ts", "src/persistence/managedRepoState.ts", "src/ui/classification.ts", "test/domain/intake/classifyRepoState.test.ts", "test/fs/managedRepo.test.ts", "test/integration/intake-classification.test.ts", "scripts/smoke/intake-classification.sh"]
autonomous: true
requirements: ["REPO-02"]
user_setup: []
must_haves:
  truths:
    - "Repo classification distinguishes fresh, in-progress, and already ported states using evidence thresholds rather than treating `.planning/yolo-port/` alone as completion."
    - "Incomplete or conflicting signals produce a recommended state and an explicit confirmation gate instead of a silent guess."
    - "Local intake leads with repo cleanliness and detected state before deeper prompts."
    - "Already ported repos surface the first-action menu in the agreed order, while later-phase actions are routed honestly instead of pretending to be implemented."
  artifacts:
    - path: "src/domain/intake/classifyRepoState.ts"
      provides: "Pure conservative repo-state classifier"
      exports: ["classifyRepoState"]
    - path: "src/adapters/fs/managedRepo.ts"
      provides: "Managed-state and prior-artifact reader for yolo-port repositories"
      exports: ["readManagedRepoState"]
    - path: "src/ui/classification.ts"
      provides: "Repo-status and recommended-state rendering"
      min_lines: 40
    - path: "test/integration/intake-classification.test.ts"
      provides: "End-to-end proof for fresh, in-progress, and already-ported intake behavior"
      contains: "already ported"
    - path: "scripts/smoke/intake-classification.sh"
      provides: "CLI smoke coverage for repo-state classification"
      contains: "in-progress"
  key_links:
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/adapters/fs/managedRepo.ts"
      via: "managed breadcrumb and prior-artifact read before classification"
      pattern: "readManagedRepoState"
    - from: "src/cli/commands/bootstrap.ts"
      to: "src/domain/intake/classifyRepoState.ts"
      via: "recommended repo-state calculation"
      pattern: "classifyRepoState"
    - from: "src/adapters/fs/managedRepo.ts"
      to: "src/persistence/managedRepoState.ts"
      via: "typed evidence model for markers and completion artifacts"
      pattern: "ManagedRepoState|CompletionEvidence"
---

<objective>
Build conservative repo-state classification and breadcrumb detection so intake can distinguish fresh, in-progress, and already ported repositories and present honest next actions.

Purpose: Phase 2 is not credible if it misreads partial yolo-port state as a completed port. Classification needs strong evidence thresholds and a transparent UI before later parity and audit phases land.
Output: A managed-state reader, a conservative classifier, a repo-status screen that leads with cleanliness and detected state, and smoke coverage for fresh, in-progress, and already-ported scenarios.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-repository-intake-and-workflow-control/02-CONTEXT.md
@.planning/phases/02-repository-intake-and-workflow-control/02-RESEARCH.md
@.planning/phases/01-bootstrap-and-foundations/01-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Model classification evidence and conservative repo-state decisions</name>
  <files>src/domain/intake/types.ts, src/domain/intake/classifyRepoState.ts, src/persistence/managedRepoState.ts, test/domain/intake/classifyRepoState.test.ts</files>
  <action>Define the typed evidence model for repo classification: managed marker presence, source-reference presence, completion-artifact presence, ambiguous or conflicting markers, and current repo cleanliness. Implement a pure classifier that returns the recommended state, the evidence that led there, whether confirmation is required, and which follow-up actions should be shown. Make `already ported` require strong completion evidence and treat partial breadcrumbs as `in-progress`. Include unit tests that prove Phase 1-only repos do not over-classify as complete and that conflicting evidence falls back to confirmation.</action>
  <verify>`bun test test/domain/intake/classifyRepoState.test.ts`</verify>
  <done>The classification rules are explicit, future-proof, and conservative enough that later phases can add richer evidence without changing the top-level decision model.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Read yolo-port breadcrumbs and prior artifacts through one filesystem adapter</name>
  <files>src/adapters/fs/managedRepo.ts, test/fs/managedRepo.test.ts</files>
  <action>Build the filesystem boundary that reads `.planning/yolo-port/manifest.json`, `bootstrap-state.json`, saved summaries, and future-proof completion signals without spreading ad hoc path logic through the CLI. It should tolerate missing files cleanly, parse only machine-owned data under `.planning/yolo-port/`, and surface prior summary paths or available action targets for the UI. Keep this adapter read-only in Phase 2; classification should depend on evidence collection, not on opportunistic mutations.</action>
  <verify>`bun test test/fs/managedRepo.test.ts`</verify>
  <done>The CLI has one place to gather managed-state evidence, and later phases can build on that reader instead of rediscovering file conventions.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Wire repo-status output, confirmation gates, and already-ported action menus</name>
  <files>src/cli/commands/bootstrap.ts, src/ui/classification.ts, test/integration/intake-classification.test.ts, scripts/smoke/intake-classification.sh</files>
  <action>Integrate the classifier into the intake flow so the first screen for local repos emphasizes cleanliness and detected state. When the evidence is mixed, show the recommended classification and ask for confirmation rather than auto-advancing silently. For already-ported repos, present actions in the agreed order: view previous run summary, audit parity against source, update from upstream, then inspect managed artifacts or state. Because audit and update execution live in later phases, route those actions honestly to placeholder messages or later-phase command surfaces instead of inventing behavior. Add integration coverage and a smoke script that prove fresh, in-progress, and already-ported repos each get the correct state and action treatment.</action>
  <verify>`bun test test/integration/intake-classification.test.ts` and `bash scripts/smoke/intake-classification.sh`</verify>
  <done>Classification is transparent to the user, ambiguous states are confirmed explicitly, and already-ported repos surface the right next actions without claiming future capabilities already exist.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test`
- [ ] `bash scripts/smoke/intake-classification.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Fresh, in-progress, and already-ported repos are distinguished by explicit evidence thresholds
- Mixed or conflicting signals trigger a recommendation-plus-confirmation path
- Local intake screens lead with repo status and detected state
- Already-ported action menus are ordered correctly and stay honest about later-phase behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-intake-and-workflow-control/02-02-SUMMARY.md`
</output>
