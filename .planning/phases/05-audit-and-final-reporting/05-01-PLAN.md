---
phase: 05-audit-and-final-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/persistence/reporting.ts", "src/domain/audit/parity.ts", "src/adapters/system/git.ts", "src/cli/commands/audit.ts", "src/cli/router.ts", "test/domain/audit/parity.test.ts", "test/system/git.test.ts", "test/integration/audit-command.test.ts"]
autonomous: true
requirements: ["AUDT-01"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260412T132755Z
generated_at: 2026-04-12T13:27:55.494Z
must_haves:
  truths:
    - "The visible audit command can run directly against an already ported managed repo."
    - "The parity audit evaluates the current repo against the saved Phase 3 checklist rather than guessing from current code alone."
    - "Git-backed source-reference tags contribute diff stats when available, but the audit still works without them."
  artifacts:
    - path: "src/persistence/reporting.ts"
      provides: "parity-audit and final-report record schemas"
      exports: ["createParityAuditRecord"]
    - path: "src/domain/audit/parity.ts"
      provides: "saved-checklist vs current-snapshot parity evaluation"
      exports: ["buildParityAudit"]
    - path: "src/cli/commands/audit.ts"
      provides: "real audit command"
      exports: ["createAuditCommand"]
  key_links:
    - from: "src/cli/commands/audit.ts"
      to: "src/domain/audit/parity.ts"
      via: "current-state audit evaluation"
      pattern: "buildParityAudit"
    - from: "src/cli/commands/audit.ts"
      to: "src/adapters/system/git.ts"
      via: "source-reference diff statistics"
      pattern: "readGitDiffStats"
---

<objective>
Implement the real parity-audit flow for already ported repositories.

Purpose: Phase 5 starts by making the visible `audit` command real. The audit must compare the current repo against the saved Phase 3 parity contract and produce a reusable parity-audit result.
Output: Reporting schemas, parity-audit domain logic, git diff statistics, and an executable `audit` command.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-audit-and-final-reporting/05-CONTEXT.md
@.planning/phases/05-audit-and-final-reporting/05-RESEARCH.md
@src/adapters/fs/portPlanning.ts
@src/adapters/fs/repositorySnapshot.ts
@src/domain/parity/inventory.ts
@src/domain/parity/checklist.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define reporting schemas and parity-audit domain logic</name>
  <files>src/persistence/reporting.ts, src/domain/audit/parity.ts, test/domain/audit/parity.test.ts</files>
  <read_first>src/persistence/portPlanning.ts, src/domain/parity/checklist.ts</read_first>
  <action>Define versioned schemas for parity-audit items, audit summaries, and final-report records. Implement a pure parity-audit function that takes the saved checklist and a fresh current repo snapshot, then marks each surface as verified or missing with a concise reason and category-level counts. Keep the audit deterministic and static, and cover the obvious parity-sensitive categories in the unit test.</action>
  <verify>`bun test test/domain/audit/parity.test.ts`</verify>
  <acceptance_criteria>
    - parity-audit records are versioned and machine-readable
    - audit results include per-surface status plus aggregate counts
  </acceptance_criteria>
  <done>Phase 5 has a typed, deterministic parity-audit layer that reuses the saved Phase 3 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add git diff stats and the real audit command</name>
  <files>src/adapters/system/git.ts, src/cli/commands/audit.ts, src/cli/router.ts, test/system/git.test.ts, test/integration/audit-command.test.ts</files>
  <read_first>src/adapters/system/git.ts, src/cli/commands/resume.ts</read_first>
  <action>Extend the git adapter with source-reference diff stats against the preserved tag when it exists, and keep the result nullable when it does not. Then implement the `audit` command so it reads the saved planning artifacts, scans the current repo, runs the parity audit, and prints a concise terminal summary. Register the command in the router and add integration coverage proving the command writes fresh audit artifacts for a managed repo.</action>
  <verify>`bun test test/system/git.test.ts` and `bun test test/integration/audit-command.test.ts`</verify>
  <acceptance_criteria>
    - `audit` is executable from the command registry
    - git-backed audits include diff stats without crashing when the source-reference tag is absent
  </acceptance_criteria>
  <done>Already ported repos can be audited directly from the CLI instead of seeing a placeholder action.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/domain/audit/parity.test.ts`
- [ ] `bun test test/system/git.test.ts`
- [ ] `bun test test/integration/audit-command.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The audit command is real and reusable
- Parity audit compares current state against the saved contract rather than current heuristics alone
</success_criteria>

<output>
After completion, create `.planning/phases/05-audit-and-final-reporting/05-01-SUMMARY.md`
</output>
