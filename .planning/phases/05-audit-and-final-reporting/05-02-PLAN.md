---
phase: 05-audit-and-final-reporting
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["src/adapters/fs/reporting.ts", "src/domain/reporting/finalReport.ts", "src/ui/audit.ts", "test/domain/reporting/finalReport.test.ts", "test/fs/reporting.test.ts"]
autonomous: true
requirements: ["RPRT-01"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260412T132755Z
generated_at: 2026-04-12T13:27:55.494Z
must_haves:
  truths:
    - "The final report combines parity status, estimate-versus-actual execution data, unresolved risks, and next steps in one reusable artifact."
    - "The report flow persists both machine-readable and human-readable outputs under .planning/yolo-port."
    - "Estimate-vs-actual data is derived from saved planning and execution artifacts rather than hand-written prose."
  artifacts:
    - path: "src/adapters/fs/reporting.ts"
      provides: "filesystem boundary for parity-audit and final-report artifacts"
      exports: ["writeReportingArtifacts"]
    - path: "src/domain/reporting/finalReport.ts"
      provides: "final report composition from audit, estimate, execution, and git diff data"
      exports: ["buildFinalReport"]
    - path: "src/ui/audit.ts"
      provides: "CLI-friendly audit and final-report summary rendering"
      exports: ["renderAuditSummary"]
  key_links:
    - from: "src/cli/commands/audit.ts"
      to: "src/domain/reporting/finalReport.ts"
      via: "final report composition"
      pattern: "buildFinalReport"
    - from: "src/domain/reporting/finalReport.ts"
      to: "src/adapters/fs/reporting.ts"
      via: "persistent report artifacts"
      pattern: "writeReportingArtifacts"
---

<objective>
Generate reusable final-report artifacts from the parity audit, saved plan estimate, and managed execution state.

Purpose: A parity audit alone is not enough; the user needs one comprehensive report that explains outcomes, deltas, and next steps.
Output: Final-report domain logic, report artifact writing, and CLI summary rendering for the report flow.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-audit-and-final-reporting/05-CONTEXT.md
@.planning/phases/05-audit-and-final-reporting/05-RESEARCH.md
@src/adapters/fs/portPlanning.ts
@src/adapters/fs/executionState.ts
@src/persistence/executionState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the final-report domain model and artifact writer</name>
  <files>src/domain/reporting/finalReport.ts, src/adapters/fs/reporting.ts, test/domain/reporting/finalReport.test.ts, test/fs/reporting.test.ts</files>
  <read_first>src/persistence/portPlanning.ts, src/persistence/executionState.ts</read_first>
  <action>Implement the final-report builder that combines the parity audit, saved estimate, execution-state timing, execution summary, and optional git diff stats into one structured report. Include parity status, estimate-versus-actual timing/cost context, unresolved risks, and next-step guidance. Add the reporting filesystem boundary that writes reusable JSON and markdown artifacts under `.planning/yolo-port/`, and cover both domain and filesystem behavior in tests.</action>
  <verify>`bun test test/domain/reporting/finalReport.test.ts` and `bun test test/fs/reporting.test.ts`</verify>
  <acceptance_criteria>
    - final-report records include parity status, estimate-vs-actual data, risks, and next steps
    - report artifacts are written as both JSON and markdown under `.planning/yolo-port/`
  </acceptance_criteria>
  <done>Phase 5 can generate a reusable final-report artifact pair from saved machine state instead of stitching together prose ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI summary rendering for audit and final-report output</name>
  <files>src/ui/audit.ts, src/cli/commands/audit.ts</files>
  <read_first>src/ui/planning.ts, src/ui/execution.ts</read_first>
  <action>Create concise default CLI renderers for audit and final-report output that show parity status, verified vs missing counts, estimate-versus-actual highlights, unresolved risks, and the final-report artifact path. Support a verbose path that prints the missing/manual-review surfaces directly without overwhelming the default mode. Integrate that renderer into the audit command after the report is written.</action>
  <verify>`bun test test/integration/audit-command.test.ts`</verify>
  <acceptance_criteria>
    - default audit output is concise and points at the final report artifact
    - verbose mode prints the highest-signal findings directly
  </acceptance_criteria>
  <done>The audit command is useful as a terminal experience, not just as a file writer.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test test/domain/reporting/finalReport.test.ts`
- [ ] `bun test test/fs/reporting.test.ts`
- [ ] `bun test test/integration/audit-command.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The final report is reusable and comprehensive
- Audit command output is concise by default and detailed when requested
</success_criteria>

<output>
After completion, create `.planning/phases/05-audit-and-final-reporting/05-02-SUMMARY.md`
</output>
