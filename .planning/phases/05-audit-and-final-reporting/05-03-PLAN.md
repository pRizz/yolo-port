---
phase: 05-audit-and-final-reporting
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: ["src/ui/classification.ts", "src/adapters/fs/managedRepo.ts", "src/cli/commands/bootstrap.ts", "src/ui/help.ts", "test/integration/intake-classification.test.ts", "test/ui/renderers.test.ts", "scripts/smoke/audit-command.sh", "scripts/smoke/intake-classification.sh"]
autonomous: true
requirements: ["AUDT-01", "RPRT-01"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260412T132755Z
generated_at: 2026-04-12T13:27:55.494Z
must_haves:
  truths:
    - "Already-ported repo UX reflects that audit is now real instead of planned."
    - "The preferred previous summary surface for completed repos is the final report when present."
    - "Audit and report behavior is covered by integration and smoke tests."
  artifacts:
    - path: "scripts/smoke/audit-command.sh"
      provides: "shell-level audit and final-report verification"
      contains: "final-report.md"
    - path: "src/ui/classification.ts"
      provides: "updated already-ported action labels"
      contains: "Audit parity against source"
    - path: "src/adapters/fs/managedRepo.ts"
      provides: "summary path preference that includes final reports"
      exports: ["readManagedRepoState"]
  key_links:
    - from: "src/ui/classification.ts"
      to: "src/adapters/fs/managedRepo.ts"
      via: "preferred previous-summary surface for completed repos"
      pattern: "Latest summary"
    - from: "scripts/smoke/audit-command.sh"
      to: "src/cli/commands/audit.ts"
      via: "real audit command shell coverage"
      pattern: "audit"
---

<objective>
Polish the already-ported UX and add shell-level audit/report verification.

Purpose: Phase 5 should finish by making the completed-repo experience coherent: real audit labels, useful summary preference, and shell-level proof that the audit/report flow works on demand.
Output: Updated already-ported UI, managed-state summary preference improvements, and smoke coverage for audit/report generation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-audit-and-final-reporting/05-CONTEXT.md
@.planning/phases/05-audit-and-final-reporting/05-RESEARCH.md
@src/ui/classification.ts
@src/adapters/fs/managedRepo.ts
@test/integration/intake-classification.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish already-ported summary and action labels</name>
  <files>src/ui/classification.ts, src/adapters/fs/managedRepo.ts, src/cli/commands/bootstrap.ts, src/ui/help.ts, test/integration/intake-classification.test.ts, test/ui/renderers.test.ts</files>
  <read_first>src/ui/classification.ts, src/adapters/fs/managedRepo.ts</read_first>
  <action>Update the already-ported classification UI so it stops labeling audit as planned and prefers the saved final report as the “previous run summary” surface when present. If no final report exists, fall back to execution summary or phase summaries. Keep the commandless local flow honest: it should still show the actions and stop, but the actions now reflect real capability. Update the related integration and renderer tests to match the final UX.</action>
  <verify>`bun test test/integration/intake-classification.test.ts` and `bun test test/ui/renderers.test.ts`</verify>
  <acceptance_criteria>
    - already-ported action labels reflect the real audit command
    - latest summary prefers final-report artifacts when present
  </acceptance_criteria>
  <done>Completed repos surface the right next actions and summary target without stale “planned” language.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell-level audit/report smoke coverage</name>
  <files>scripts/smoke/audit-command.sh, scripts/smoke/intake-classification.sh</files>
  <read_first>scripts/smoke/bootstrap-execution.sh, scripts/lib/run-logging.sh</read_first>
  <action>Create an audit smoke script that seeds a managed repo with planning and execution artifacts, runs `yolo-port audit`, and verifies `parity-audit` plus `final-report` outputs. Update the existing intake-classification smoke script to assert the audit label is no longer marked planned. Keep both scripts breadcrumb-heavy and compatible with the existing run-log conventions.</action>
  <verify>`bash scripts/smoke/audit-command.sh` and `bash scripts/smoke/intake-classification.sh`</verify>
  <acceptance_criteria>
    - audit smoke proves the final-report artifacts are written and reusable
    - classification smoke reflects the real audit label
  </acceptance_criteria>
  <done>Phase 5 has shell-level proof for both the real audit command and the polished already-ported UX.</done>
</task>

</tasks>

<verification>
- [ ] `bun x tsc --noEmit`
- [ ] `bun test`
- [ ] `bash scripts/smoke/audit-command.sh`
- [ ] `bash scripts/smoke/intake-classification.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Already-ported UX surfaces the real audit flow cleanly
- Audit/report behavior has shell-level proof
</success_criteria>

<output>
After completion, create `.planning/phases/05-audit-and-final-reporting/05-03-SUMMARY.md`
</output>
